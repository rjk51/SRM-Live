import 'package:flutter/material.dart';

class EventRegister extends StatelessWidget {
  const EventRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Event Register'),
      ),
      body: const Center(
        child: Text('Event Register'),
      ),
    );
  }
}